apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sandbox-api.fullname" . }}-test-connection"
  labels:
    {{- include "sandbox-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sandbox-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
